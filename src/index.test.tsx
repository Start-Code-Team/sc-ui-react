import '@testing-library/jest-dom';

import SCButton from './components/SCButton/SCButton';

describe('SC-UI-React Library', () => {
  it('Exportacion del componente SCButton', () => {
    expect(SCButton).toBeDefined();
  });
});
